{
  "name": "sarh-alwaleed",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Sarh Al-Waleed real estate services — unified electronic rental contracts (Saudi Arabia)",
  "workspaces": [
    "backend"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "npm --prefix backend run start",
    "dev": "npm --prefix backend run dev",
    "demo": "npm --prefix backend run demo",
    "seed": "npm --prefix backend run seed",
    "create-admin": "npm --prefix backend run create-admin --",
    "test": "npm --prefix backend run test",
    "lint": "npm --prefix backend run lint && npm run lint:frontend",
    "lint:frontend": "eslint frontend/js --no-error-on-unmatched-pattern",
    "typecheck": "npm --prefix backend run typecheck && npm run check:frontend",
    "check:frontend": "node scripts/check-frontend.mjs",
    "build": "npm run typecheck && npm run lint && npm run scan:secrets && npm test",
    "scan:secrets": "node scripts/scan-secrets.mjs",
    "scan:staged": "node scripts/scan-secrets.mjs --staged"
  },
  "devDependencies": {
    "eslint": "^9.17.0",
    "globals": "^15.14.0"
  }
}
